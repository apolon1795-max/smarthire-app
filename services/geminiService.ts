
import { TestResult, CandidateInfo, CustomTestConfig } from "../types";

// Единый источник правды для URL скрипта
export const SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbxEsHd6tfjTlNqBHERiJ_dUQgk9YOBntn2aD94eEUzy-MjN2FPPgTwkDzTSCy-_9p7k/exec';

const callBackendAI = async (prompt: string, jsonMode: boolean = false): Promise<string> => {
  try {
    const response = await fetch(SCRIPT_URL, {
      method: 'POST',
      headers: { 'Content-Type': 'text/plain;charset=utf-8' },
      body: JSON.stringify({
        action: 'GENERATE_AI',
        prompt: prompt,
        jsonMode: jsonMode
      })
    });
    
    const text = await response.text();
    
    if (text.trim().startsWith('<')) {
        throw new Error("Ошибка доступа к скрипту Google. Проверьте Deployment.");
    }

    const data = JSON.parse(text);
    if (data.status === 'success') {
      return data.text;
    } else {
      throw new Error(data.message || "Ошибка генерации");
    }
  } catch (error: any) {
    throw new Error(error.message || "Ошибка сети");
  }
};

export const generateCandidateProfile = async (results: TestResult[], candidateInfo?: CandidateInfo): Promise<string> => {
  const resultsText = results.map(r => {
    let details = '';
    if (r.sectionId === 'conscientiousness') {
       details = ` (HEXACO: ${r.hexacoProfile?.map(h => `${h.code}=${h.average}`).join(', ')})`;
    }
    if (r.sectionId === 'motivation') {
       details = ` (Drivers: ${r.motivationProfile?.topDrivers.map(d => d.name).join(', ')})`;
    }
    return `- ${r.title}: ${r.percentage.toFixed(0)}%${details}`;
  }).join('\n');

  const prompt = `
    Роль: Старший эксперт по оценке талантов и организационный психолог.
    Задача: Создать аналитический отчет по кандидату на основе данных тестов.
    Язык: Русский (Строгий бизнес-стиль).
    Кандидат: ${candidateInfo?.name || "Кандидат"}, Вакансия: ${candidateInfo?.role || "Соискатель"}.
    
    Результаты:
    ${resultsText}
    
    ТРЕБОВАНИЯ:
    1. Глубокий синтез данных. Не пересказывай цифры, а делай выводы о поведении.
    2. Используй HTML: <h3> для заголовков, <b> для акцентов, <ul>/<li> для списков.
    3. Структура:
       <h3>💡 Резюме и Прогноз</h3> - Подходит ли человек под роль? Каков его стиль работы?
       <h3>🚀 Ключевые компетенции</h3> - Что он делает лучше других?
       <h3>⚠️ Риски и Ограничения</h3> - В каких ситуациях он может "сломаться" или потерять эффективность?
    
    ВАЖНО: Верни ТОЛЬКО HTML. Без разметки markdown.
  `;

  try {
    return await callBackendAI(prompt, false);
  } catch (e: any) {
    return `<div style='color:#f87171;'>Ошибка AI анализа: ${e.message}</div>`;
  }
};

export const generateCustomQuestions = async (jobRole: string, challenges: string): Promise<CustomTestConfig | null> => {
  const prompt = `
    ТЫ - ВЕДУЩИЙ АРХИТЕКТОР АССЕССМЕНТ-ЦЕНТРОВ. Твоя задача — создать ПРЕДЕЛЬНО СЛОЖНЫЙ и реалистичный тест для позиции "${jobRole}". 
    Забудь про простые вопросы. Нам нужны бизнес-кейсы, где нет однозначно правильного ответа, и кандидат должен проявить мастерство приоритетов.
    Язык: Русский (Деловой). Контекст: "${challenges}".

    *** ЧАСТЬ 1: SJT (Ситуационный тест) - 4 Вопроса ***
    ИНСТРУКЦИЯ К ВОПРОСАМ:
    1. Сценарий должен быть объемным (80-120 слов). Опиши атмосферу, внешнее давление и внутренний конфликт.
    2. Используй "Систему Тройной Дилеммы":
       - Вариант 2 балла (Стратегический): Действие, которое решает проблему, сохраняет лицо компании и не нарушает критические бизнес-процессы. Требует эмпатии и смекалки.
       - Вариант 1 балл (Формальный): Следование букве закона/инструкции в ущерб лояльности или долгосрочной выгоде.
       - Вариант 0 баллов (Реактивный): Пассивность, агрессия, перекладывание ответственности или "покупка" спокойствия клиента за счет убытков компании.
    
    ЗАПРЕЩЕНО: Использовать варианты вроде "Я накричу на клиента" или "Я просто уйду". Все варианты должны выглядеть профессионально.

    *** ЧАСТЬ 2: WORK SAMPLE (Практический кейс) - 1 Задание ***
    Создай ПОЛНОЦЕННЫЙ КЕЙС-СТАДИ. 
    Структура поля "text" должна быть такой:
    "ЛЕГЕНДА: [Масштабная ситуация на 4-5 предложений].
    
    ТЕКУЩИЕ ОБСТОЯТЕЛЬСТВА:
    - [Факт 1: Ограничение ресурсов]
    - [Факт 2: Прямая угроза репутации/прибыли]
    - [Факт 3: Противоречивое указание от руководства]
    
    ЗАДАНИЕ ДЛЯ ВАС:
    [Развернутая инструкция: составить план, написать ответное письмо, расставить приоритеты из 5 задач с обоснованием]."

    ФОРМАТ ВЫДАЧИ (ТОЛЬКО JSON):
    {
      "sjtQuestions": [
        { 
          "id": "1", 
          "text": "[Длинное описание ситуации с деталями]", 
          "type": "scenario", 
          "options": [
             { "id": "a", "text": "[Стратегическое решение]", "value": 2 }, 
             { "id": "b", "text": "[Формальное решение]", "value": 1 },
             { "id": "c", "text": "[Неэффективное решение]", "value": 0 }
          ] 
        }
      ],
      "workSampleQuestion": { 
        "id": "ws1", 
        "text": "[Весь текст Кейс-Стади: Легенда + Обстоятельства + Задание]", 
        "type": "text" 
      }
    }
  `;
  
  try {
    const jsonString = await callBackendAI(prompt, true);
    const cleanJson = jsonString.replace(/```json/g, "").replace(/```/g, "").trim();
    return JSON.parse(cleanJson);
  } catch (error) {
    console.error("Custom Question Gen Error:", error);
    throw error;
  }
};
