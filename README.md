# Портал Оценки Кандидатов (SmartHire Edition)

## 📌 Описание проекта
SPA (Single Page Application) для комплексного ассессмента кандидатов. Приложение объединяет психометрику, когнитивные тесты и генеративный ИИ для создания "Паспорта кандидата".

## 🛠 Технологический стек
*   **Frontend:** React 19, Tailwind CSS (Dark Mode).
*   **AI:** Google GenAI SDK (`gemini-2.5-flash`).
*   **Backend / Database:** Google Apps Script + Google Sheets.

---

## 🏆 Интеграция: Google Sheets Database
Мы успешно внедрили архитектуру Serverless сохранения данных. Вместо ненадежной генерации PDF на клиенте, приложение отправляет данные напрямую в Google Таблицу.

**Статус:** ✅ Stable (Проверено)

### Как это работает:
1.  **Frontend:** Собирает результаты (IQ, HEXACO, Мотивация) и текст от AI.
2.  **Transport:** Отправляет JSON через `fetch` (mode: no-cors) на Webhook Google Script.
3.  **Backend:** Скрипт принимает данные, чистит HTML-теги и добавляет новую строку в таблицу.

---

## ⚙️ Инструкция по настройке Бэкенда

Код бэкенда находится в файле: `backend/google-script.js`.

### 1. Установка скрипта
1.  Перейдите на [script.google.com](https://script.google.com/).
2.  Создайте новый проект.
3.  Скопируйте содержимое `backend/google-script.js` в редактор.
4.  **Важно:** Вставьте ID вашей таблицы в переменную `sheetId`.

### 2. Развертывание (Критически важно!)
Google Script не обновляется автоматически. При любом изменении кода:
1.  Нажмите синюю кнопку **"Начать развертывание"**.
2.  Выберите **"Управление развертываниями"**.
3.  Нажмите иконку **Карандаша** (Редактировать).
4.  В выпадающем списке "Версия" выберите **"Новая версия"**.
5.  Нажмите **"Развернуть"**.

### 3. Отладка (Если таблица пустая)
В скрипт встроена функция `testDebug()` для проверки прав доступа без участия React.
1.  В редакторе скриптов выберите `testDebug` в списке функций (сверху).
2.  Нажмите **"Выполнить"**.
3.  Если Google запросит разрешения — выдайте их (Review Permissions -> Allow).
4.  В журнале должно появиться: `"Строка успешно добавлена!"`.

---

## 🚀 Запуск Frontend
1.  Открыть `index.html` (или запустить через локальный сервер).
2.  Убедиться, что в `components/ResultsView.tsx` указан правильный `WEBHOOK_URL` вашего развернутого скрипта.
